# Launch Follow-Ups

These items are intentionally deferred from the initial `v1.0.0` repository polish. They should not block publishing if the README stays honest about the current state.

## Deferred

- [x] Replace the static `assets/demo-terminal.svg` placeholder with an animated replay backed by `assets/demo.cast` and `assets/demo-session.txt`.
- [x] Add a manual run snapshot for all 15 expected-behavior cases under `tests/prompt-xray-runs/`.
- [x] Add a public 15-case benchmark scorecard in `docs/benchmark.md`.
- [ ] Re-run all 15 expected-behavior cases in a fresh public release session before cutting a later release.
- [ ] Add named injection probes for Test mode, such as `injection-probe.override`, `injection-probe.tool-call`, and `injection-probe.exfil`.
- [ ] Add repository topics after publishing: `prompt-engineering`, `agent-skills`, `codex`, `openclaw`, `hermes-agent`, `claude-code`, `prompt-injection`.
- [ ] Open GitHub Discussions after the first release if there is real usage or feedback.
